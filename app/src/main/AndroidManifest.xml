<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CompouseUIApplication"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.CompouseUIApplication">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".basic.Lesson1Activity"/>
        <activity android:name=".basic.Lesson2Activity"/>
        <activity android:name=".basic.Lesson3Activity"/>
        <activity android:name=".basic.Lesson4Activity"/>
        <activity android:name=".basic.Lesson5Activity"/>
        <activity android:name=".basic.Lesson6Activity"/>

        <activity android:name=".advanced.RequestSampleActivity"/>
        <activity android:name=".advanced.EffectActivity"/>
    </application>

</manifest>